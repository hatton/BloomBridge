{
  "name": "@bloombridge/gui",
  "version": "1.0.0",
  "private": true,
  "description": "BloomBridge — local web GUI for managing conversions",
  "type": "module",
  "scripts": {
    "dev": "vp dev",
    "build": "vp build",
    "build:server": "esbuild server/serve.ts --bundle --platform=node --format=cjs --packages=external --outfile=server-dist/serve.cjs",
    "preview": "vp preview"
  },
  "dependencies": {
    "@bloombridge/lib": "workspace:*",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@types/node": "^22.15.24",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^5.2.0",
    "esbuild": "^0.21.5",
    "typescript": "^5.8.3",
    "vite": "catalog:",
    "vite-plus": "catalog:"
  }
}
