# pnpm
node_modules/
.pnpm-store/

# Vite+ task cache
.vite/cache/

dist/

test-inputs/**/index.html
testme/**
# Anchored to the repo root so it only ignores a top-level test-output "collections"
# dir — an un-anchored "collections/" also matched packages/lib/src/collections/ and
# silently kept that source file out of git (broke clean-checkout builds on CI).
/collections/

bilingual-sample_bloom/

bilingual-sample.tagged_bloom/

test-output/
test-outputs/

debug*.js
poppler-24.08.0/
large-local-test-inputs/
marker/
